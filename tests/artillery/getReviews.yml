config:
  target: "http://localhost:3000"
  payload:
    path: "../data/products.csv"
    fields:
      - "id"
  phases:
    - duration: 10
      arrivalRate: 20
  defaults:

scenarios:
  - flow:
      - get:
          url: "/reviews/{{id}}/list"
